The "workarea" page template generates the common header and footer areas,
as well as the site-common sidebar.  The page fragment will be inserted
into the workarea (usually next to the sidebar).
Pages defined here will have a common sidebar.
