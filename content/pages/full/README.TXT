The "full" page template generates only the common header and footer areas,
with the page fragment inserted in-between.  Pages defined here will
take up the entire content area, and will not have the default sidebar or
other secondary navigation.
